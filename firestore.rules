rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The browser has no Firestore data plane. All financial reads and writes
    // go through the HMAC-authenticated server API and Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
